# 專案協作規則

## 開始工作前

先讀 [系統規則與知識總覽](docs/系統規則與知識總覽.md) 第 1 節核心規則，再依該節索引讀取與本次任務相關的章節、實作與測試；不再要求每次或每個 Agent 重讀全文。

- 總覽是現行規則的統一入口；不要只依對話記憶、README 或歷史 POC 文件修改。
- 同一任務已讀且未變動的內容不重讀；新接手者仍須讀核心規則，並核對交接中的相關來源。
- 入口無法讀取時先恢復；文件、程式與最新需求有實質差異時先查證，再釐清。
- 使用者最新明確需求優先；規則改變時同步更新總覽、相關文件與一句話專案日誌。
- 保留其他任務的既有異動；如實區分開發、測試、部署及人工驗收狀態。

## Agent 開發流程

遵循 [Codex 開發流程](docs/Codex開發流程.md)。

- 一般程式開發由主 Agent 規劃、實作及測試，完成後委派一位 verifier 獨立驗證；單純問答、文件、格式及 Agent 設定調整由主 Agent 檢查即可。
- 大型跨模組或需求仍有實質歧義時，才按需要委派 planner／implementer；不固定啟動三個角色，也不層層再委派。
- 派工必須明確選用 agent_type（planner／implementer／verifier）；task_name 只是名稱。預設 fork_turns="none"，附足夠的需求、檔案責任、驗收條件及證據位置。
- 預設保留 Astra／high；不為例行工作自動升級 xhigh。明確的使用者模型或推理選擇優先。
- 實作只有一位寫入負責人；驗證時凍結其檢查範圍。主 Agent 不重做已委派的同一份工作。
- 初次派工及交接以約 500 字摘要與證據路徑為目標；主 Agent 的協調摘要只列當前階段及待決事項，依平台要求簡短回報並使用事件等待，不額外查詢、催問或重產未變證據。
- 同一根因連續兩輪修正與獨立複驗仍未解，先依流程檢討設計與正反案例，再決定修法；版本改名不重置輪次，既有授權、驗收及停修限制持續有效。
- 先預檢環境與前置條件，在必要控制完成後用代表性高風險案例確認可行性；中間修正先離線／重播及定向檢查，最終候選凍結前完成必要完整離線檢查。付費全量依影響與授權預算決定，既有暫停不因流程重設解除。

## CodeGraph

專案根目錄存在 .codegraph/ 時，理解或定位程式先使用 CodeGraph；沒有時不自行建立索引，一般搜尋優先用 rg／rg --files。

## 檔案格式

新增或修改程式、設定與文件一律維持 UTF-8、Windows CRLF（\r\n）。

## 本機 Shell

- 本機終端命令預設使用 Windows 內建的 `powershell.exe`，不假定已安裝 `pwsh`；需要其他 shell 時先檢查是否存在。供 Windows PowerShell 5.1 執行且含中文的 `.ps1` 使用 UTF-8 BOM 與 CRLF，命令路徑完整保留分隔符。
