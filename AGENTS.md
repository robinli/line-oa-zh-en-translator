# 專案協作規則

## 開始工作前必讀

開始本專案任何新工作前，尤其修改程式、設定、模型提示、測試或部署流程前，**必須先完整閱讀 [系統規則與知識總覽](docs/系統規則與知識總覽.md)**，再閱讀受影響的程式與細節文件。不要只依對話記憶、README 摘要或舊 POC 文件開始修改。

- 總覽是本專案現行規則與知識的統一入口；歷史文件中已被取代的說法不能覆蓋現行需求。
- 使用者最新明確需求調整規則時，依要求執行，並在同一次工作中更新總覽、相關文件及專案日誌。
- 若總覽不存在或無法讀取，先恢復／釐清這個必讀入口，再修改程式；不要直接略過。
- 總覽不能取代實作核對；文件、程式與最新需求有出入時，先查證來源及版本，仍有實質歧義再釐清。
- 記錄開發、測試、部署及人工驗收的實際狀態，不把待辦寫成完成。

## CodeGraph

專案根目錄存在 .codegraph/ 時，需要理解或定位程式，先使用 codegraph_explore 或 codegraph explore，再視需要搜尋／閱讀原始碼。沒有 .codegraph/ 時跳過，不自行建立索引；一般搜尋優先使用 rg／rg --files。

## 檔案格式

新增或修改任何程式碼，一律使用 Windows CRLF（\r\n）換行；本專案文件亦維持 CRLF。
