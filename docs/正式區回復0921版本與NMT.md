# 正式區回復 9/21 Git 版本與 NMT

## 使用者要求與版本依據

2026-09-25 使用者明確要求不修改任何程式，直接取回 9/21 當時的 Git 版本部署，翻譯引擎恢復 NMT；已選擇完整歷史功能回復，不是只切換現行程式的引擎。

- 以 Asia/Taipei 的 2026-09-21 為日期基準；當日正式服務為 linewebhook-00010-vuz（9/14 部署）。第 11 版 UTC 雖為 9/21，換算台灣時間為 9/22，故不是本次目標。
- Git 目標為 0b4ec8378ac2aed5a728c32da9884e1313a04dfa，提交時間 2026-09-15T08:34:52+08:00，是 9/21 結束前最後一個提交；其 README 記錄第 10 版。
- 第 10 版原容器映像與當時部署來源已找不到，故從 Git 重建，不宣稱沿用相同容器 digest。
- 隔離 checkout：.local/rollback-20260925-to-0921/source（detached HEAD）。主工作區目前分支、未提交候選與獨立 dev 環境保留。
- 候選 tracked 程式、設定與 lockfile 無內容修改；checkout 採 CRLF。只在 Git 忽略的部署 .env 沿用 MAX_MESSAGE_LENGTH=2000、MAX_AUDIO_DURATION_MS=59000、MAX_AUDIO_BYTES=10000000。

## 回復後的功能範圍

| 項目 | 9/21 原版行為 |
|---|---|
| 中英／中越翻譯 | Google Cloud Translation v3 的預設 NMT；原文直接送 global translateText，不指定 LLM、Gemini 或 glossary |
| 引擎選擇 | index 直接建立 GoogleCloudTranslator，沒有 TRANSLATION_ENGINE 選擇機制 |
| 四種模式 | zh-to-en、en-to-zh、zh-en、zh-vi；文字／語音開關各自獨立 |
| 群組設定 | 單一 owner 可修改，成員可查詢；沿用 lineTranslationGroups 與現有資料 |
| 私訊 | 恢復當時的翻譯／設定功能；原 user:{userId} 歷史設定重新適用，不再只有 /我的ID |
| 舊指令 | 恢復 /啟用翻譯、/停用翻譯、/翻譯狀態 等原版相容指令 |
| 後續新增功能 | 不含商務 LLM 資料保護、glossary、原生 @／別名提及、相同譯文略過及整則 OK／Yes／No 略過 |
| 失敗 | 原版捕捉失敗並記錄 failed，沒有後來曾新增的翻譯失敗雙語提示 |

完整歷史回復是本次明確需求；不能將目前主工作區的新功能文件當作回復後正式行為。這次沒有修改聊天室設定、LINE OA、Webhook URL 或 Secret。

## 驗證與部署紀錄

- Node.js 22.23.2，從原 package-lock.json 執行 npm ci；121 項測試、型別檢查及建置通過。
- 一位 verifier 獨立確認 30 個 tracked 檔案與 commit 一致（27 個僅換行差異），並以 fake clients 驗證 NMT 四方向、設定相容、簽章、權限、方向略過及錯誤處理；零網路請求，部署前放行。
- 首次 Firebase 本機探索在 30 秒逾時，尚未更新正式；僅調整操作環境 FUNCTIONS_DISCOVERY_TIMEOUT=120 重試，沒有程式修改，重試 predeploy 再通過 121 項測試與 check/build。
- 狀態：已部署 linewebhook-00019-hid，Cloud Functions ACTIVE，Cloud Run 100% 流量；原 URL、執行帳號、三項 Secret 版本及 256 MiB／60 秒／5 實例限制核對一致。
- 本次不執行付費翻譯回歸、不向真人發送測試訊息；正式翻譯品質及 LINE 手機端驗收另計。

私密備份與證據在 .local/rollback-20260925-to-0921/：before-function.json、before-service.json、before-revision.json、before-deployed-source.zip、offline-verification.log、independent-verification.md、deployment.log、deployment-retry.log。第 18 版的部署來源已下載備份。

回復 NMT 不代表歷史費用取消，也不保證未來每日金額；本次未將截圖帳單全部認定為正式聊天費用。

- 部署後 GET 405、錯誤簽章 401、有效空事件及不支援事件略過共 4 項檢查通過，沒有翻譯 API 呼叫或 LINE 真人訊息。
- 從雲端下載實際部署 source zip，比對 12 個 src／lib JS／package／lock 檔均與隔離候選逐 byte 一致；候選 tracked diff/status 仍為空。
- 主工作區 45 個既有程式檔 hash 未變；本次僅更新維運文件與本機部署證據。
